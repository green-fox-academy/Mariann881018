3rd Week:

2st day - File IO

absolute path and relative path

exceptions are not errors,
errors stop the program,

in case of exception, if we handle it, the program can run

How to catch an exeption:
put it in a try-catch 

finally
you can use it without catch

nio package
it has several methods to handle files


you can never leave empty a catch


Integer.parsInt() (some string) - if you want  to convert a string to integers


arraylists - trim - eliminate the whitespaces
